# 빌드 산출물과 로컬 상태는 이미지에 넣지 않는다.
**/build/
**/.gradle/
.git/
.github/

# ⚠️ 사용자 인증샷이다. 이미지에 굽히면 개인정보가 배포본에 실려 나간다 —
#    실행 시 볼륨으로 붙인다 (compose 의 photos 볼륨).
**/data/photos/

# 프론트는 이 이미지가 굽지 않는다.
fishing_app/

# 비밀값은 환경변수로 주입한다. 파일이 딸려 들어가면 안 된다.
*.env
